// ─── CycleService — Web stub ──────────────────────────────────────────────────
//
// Same API as the native implementation; the web build simply has nowhere
// private to keep special-category data, so it keeps none.

class CycleService {
  static Future<String?> read() async => null;
  static Future<void> write(String value) async {}
  static Future<void> clear() async {}
}
